# LaraUpdater must be able to HTTP-fetch RELEASE-*.zip from this folder (same as LARA_UPDATER_URL).
# Blocking .zip here breaks "Update Now" with 403/404 when the app downloads from its own domain.
Options -Indexes
